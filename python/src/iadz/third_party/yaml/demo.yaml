name: iamdavidzeng

files:
    config1.json:
        {
            "level": "info",
            "format": "customize format"
        }
    config2.json:
        {
            "level": "warning",
            "format": "customize format"
        }

description: |-    # 用于换行和去除末行换行
    some description,
        e.g. data=[
            {
                "value": 10
                "sample": {
                    "foo": "bar"
                }
            },
        ]
