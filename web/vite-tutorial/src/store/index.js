import { createStore } from 'vuex';

const state = {};

const getters = {};

const actions = {};

const mutations = {};

export const store = createStore({
    state,
    getters,
    actions,
    mutations
});
